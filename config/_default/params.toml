# config/_default/params.toml
#
# Only what the templates in layouts/ actually read. Presentation that used to
# live here — appearance, header layout, per-article switches — is now in the
# templates and in assets/css/main.css.
#
# Per-page overrides, set in front matter:
#   showTableOfContents  single pages; default true, suppressed below 2 headings
#   showReadingTime      list cards; default false
#   sortBy               section lists; "title" (default) or "date"

description = "How this development environment is put together, and why."
