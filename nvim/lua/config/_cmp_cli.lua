local M = {}

M.setup = function()
  -- require('cmp-cmdline').setup.cmdline(':', {
  --   sources = cmp.config.sources({
  --     { name = 'path' }
  --   }, {
  --     { name = 'cmdline', keyword_pattern=[=[[^[:blank:]\!]*]=]}
  --   })
  -- })
end

return M
